# Pre-check pass token (generated at runtime)
.precheck-pass

# App Store Connect API key — NEVER commit
*asc-key*.json
fastlane/.env
.env

# Node
node_modules/

# Local user config (the example is committed)
.appstore-precheck.json

# Internal planning notes (kept out of the public repo)
.planning/

# OS noise
.DS_Store
.superpowers/

# LLM eval — local run outputs (committed baseline lives in eval/baseline/)
eval/runs/

# RAG corpus — full Apple guideline prose, deliberately NOT committed to this
# public repo (Apple's copyrighted text). Regenerate with eval/rag/ingest.sh;
# provenance is tracked via the file's own fetched_on/source_url fields.
eval/rag/corpus/sections.json
